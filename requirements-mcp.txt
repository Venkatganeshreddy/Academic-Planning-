# Deps for the MCP server (mcp_server.py) only. Deliberately minimal -- the server
# imports aip/db.py (duckdb) + mcp, nothing from the Streamlit app -- so the Render
# deploy stays small and fast. uvicorn/starlette come in via mcp.
duckdb>=1.1
mcp>=1.2
