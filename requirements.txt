streamlit>=1.42
starlette<1.4        # streamlit 1.61's GZip middleware breaks on starlette 1.4.0 (missing thread_minimum_size)
Authlib>=1.3.2
httpx>=0.27          # Authlib's Starlette OAuth client (what st.login uses) imports it
duckdb>=1.1
requests>=2.32
markdown>=3.5
